---
- name: Fonts - copy
  become: no
  synchronize:
    src="{{ ansible_dir }}/user/files/fonts/"
    dest="{{ home_dir}}/.fonts/"
    dirs=yes

- name: Fonts - update cache
  become: no
  command: fc-cache -f -v
